# port - listening port owner lookup

`port` shows listening TCP sockets and the owning process. With an argument, it
filters to a single local port.

## Usage

```bash
port [PORT]
```

## Examples

```bash
port
port 5432
```
