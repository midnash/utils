# git-summary - repository stats

`git-summary` prints high-level repository stats in one command.

## Usage

```bash
git-summary [REPO_PATH]
```

## Output

- total commits
- contributor count
- first commit date
- last commit date
- most changed files (top 10)

## Example

```bash
git-summary .
```
