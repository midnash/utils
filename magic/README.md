# magic - file type by signature

`magic` identifies file type by reading magic bytes from file contents, not filename extension.

## Usage

```bash
magic FILE...
```

## Example

```bash
magic /bin/ls photo.jpg archive.zip
```
