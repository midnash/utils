# elf - ELF header inspector

`elf` prints a human-readable ELF summary: type, architecture, entry point,
program headers, sections, and interpreter path when present.

## Usage

```bash
elf FILE
```

## Example

```bash
elf /bin/ls
```
